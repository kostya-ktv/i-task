export namespace DashboardNavbar {
  export const orgSwitcherTheme = {
    elements: {
      rootBox: {
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "GrayText",
      },
    },
  };
}
